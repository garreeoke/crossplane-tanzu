apiVersion: services.apps.tanzu.vmware.com/v1alpha1
kind: ResourceClaim
metadata:
  name: broker-claim
  namespace: default
spec:
  ref:
    apiVersion: v1
    kind: Secret
    name: spring-sensors-broker-rabbit